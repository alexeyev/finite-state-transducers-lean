# Keep the pinned GitHub Actions (checkout, cache, …) up to date.
# The Lean toolchain itself is pinned in lean-toolchain and updated by hand.
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
    commit-message:
      prefix: "ci"
